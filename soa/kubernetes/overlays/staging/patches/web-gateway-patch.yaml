apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-gateway
  namespace: magenta-staging
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: web-gateway
          env:
            - name: MAGENTA_ENV
              value: "staging"
            - name: MAGENTA_LOG_LEVEL
              value: "INFO"
            - name: MAGENTA_API_KEY
              valueFrom:
                secretKeyRef:
                  name: magenta-secrets
                  key: api-key
            - name: MAGENTA_REDIS_PERSISTENCE
              value: "true"
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
